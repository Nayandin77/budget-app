Tasks + Goals

- Theme 
    - Pass through so many parent -> child

- Auth.js + Home.js
    - Fix routing, so on refresh the routing works as expected     

- Amount.js
    - Update amount variable when month is changed


MAJOR
    - Make Calender send Month (props.onClick) for a newly created Month,
        if not found from within filter